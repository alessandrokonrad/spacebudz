import React from "react";
import Button from "../components/Button";

const Landing = (props) => {
  return (
    <div style={{ position: "absolute", top: 30, left: 30 }}>
      <Button type="success">Landing</Button>
    </div>
  );
};

export default Landing;
